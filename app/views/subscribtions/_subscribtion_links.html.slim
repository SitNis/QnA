- if current_user.subscribed?(question)
  p= link_to 'Unsubscribe',
    subscribtion_path(current_user.subscribed(question)),
    method: :delete,
    remote: true
- else
  p= link_to 'Subscribe',
    question_subscribtions_path(question),
    method: :post,
    remote: true
